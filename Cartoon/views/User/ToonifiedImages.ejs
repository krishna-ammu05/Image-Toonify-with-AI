<% layout('User/user_Boilerplate') -%>


<div class="container mx-auto p-6">
  <h1 class="text-3xl font-bold mb-6">🎨 My Toonified Images</h1>

  <% if (images && images.length > 0) { %>
    <!-- Responsive Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      <% images.forEach(img => { %>
        <div class="bg-white shadow-lg rounded-lg overflow-hidden hover:shadow-xl transition-shadow duration-300">
          <img src="<%= img.url %>" alt="<%= img.name %>" class="w-full h-48 object-cover">
          <div class="p-4">
            <h3 class="text-lg font-semibold truncate"><%= img.name %></h3>
            <p class="text-gray-500 text-sm">Converted on: <%= new Date(img.convertedAt).toLocaleDateString() %></p>
            <div class="mt-2 flex justify-between items-center">
              <a href="<%= img.url %>" target="_blank" class="text-blue-500 hover:underline text-sm">View</a>
              <form action="/images/delete/<%= img.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this image?');">
                <button type="submit" class="text-red-500 hover:underline text-sm">Delete</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <p class="text-gray-600">You haven’t converted any images yet. Start creating some fun toonified images!</p>
  <% } %>
</div>
