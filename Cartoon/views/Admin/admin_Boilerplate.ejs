<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>"Toonify Admin Dashboard"</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="d-flex">
    <!-- Sidebar -->
    <nav class="bg-white border-end vh-100 p-3" style="width:250px;">
      <h4 class="fw-bold mb-4">Toonify Admin</h4>
      <ul class="nav flex-column">
        <li class="nav-item"><a href="/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'text-primary fw-bold' : 'text-dark' %>">📊 Dashboard</a></li>
        <li class="nav-item"><a href="/userManagement" class="nav-link <%= activePage === 'users' ? 'text-primary fw-bold' : 'text-dark' %>">👥 Users</a></li>
        <li class="nav-item"><a href="/imageManagement" class="nav-link <%= activePage === 'images' ? 'text-primary fw-bold' : 'text-dark' %>">🖼️ Images</a></li>
        <li class="nav-item"><a href="/subscriptions" class="nav-link <%= activePage === 'subscriptions' ? 'text-primary fw-bold' : 'text-dark' %>">💳 Subscriptions</a></li>
        <li class="nav-item"><a href="/payments" class="nav-link <%= activePage === 'payments' ? 'text-primary fw-bold' : 'text-dark' %>">💰 Payments</a></li>
       
        <li class="nav-item"><a href="/logout" class="nav-link text-danger">🚪 Logout</a></li>
      </ul>
    </nav>

    <!-- Main Content -->
    <main class="flex-grow-1 p-4">
      <!-- Header -->
      <header class="d-flex justify-content-between align-items-center mb-4">
        <div class="input-group w-50">
          <input type="text" class="form-control rounded-pill" placeholder="Search...">
        </div>
        <div class="d-flex align-items-center gap-2">
          <span class="fw-semibold">Admin</span>
          <span class="text-muted">▼</span>
        </div>
      </header>

      <!-- Page Content -->
      <%- body %>
    </main>
  </div>

  <!-- Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
